import {
  DISPLAY_ALERT,
  CLEAR_ALERT,
  REGISTER_USER_BEGIN,
  REGISTER_USER_SUCCESS,
  REGISTER_USER_SUCCESS,
} from "./actions.js";

const reducer = (state, action) => {
  if (action.type === DISPLAY_ALERT) {
  }
};

export default reducer;
